Placeholder: `icon-192.png` and `icon-512.png` referenced by
`vite.config.ts`'s PWA manifest don't exist yet. Add real app icons before
this repo goes public / before relying on "Add to Home Screen" showing a
proper icon. Not required for the M0 latency/permissions proof-of-concept.
